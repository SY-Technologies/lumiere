fonction principal() {
  essayer {
    soit d = {"nom": "Ada"}
    afficher(d["ville"])
  } attraper (e: Texte) {
    afficher(e.contient("cle introuvable"))
  } finalement {
    afficher("fin")
  }
}
